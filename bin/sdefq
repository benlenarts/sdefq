#!/usr/bin/env python3
"""sdefq — query macOS AppleScript dictionaries from the command line."""

import os
import sys

# Add the repo root to the path so `import sdef` works
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from sdef.__main__ import main

main()
